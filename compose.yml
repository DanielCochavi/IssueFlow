services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: issueflow
      POSTGRES_USER: issueflow
      POSTGRES_PASSWORD: dev-only-need-to-change-before-deployment
    ports:
      - "5432:5432"
